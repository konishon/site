{
  "name": "nishon-notes",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "cloudflare:dev": "npm run build && npx wrangler dev",
    "deploy": "npm run build && npx wrangler deploy"
  },
  "dependencies": {
    "astro": "^5.12.0",
    "d3-geo": "^3.1.1",
    "lucide-static": "^1.26.0",
    "topojson-client": "^3.1.0",
    "world-atlas": "^2.0.2"
  },
  "devDependencies": {
    "wrangler": "^4.113.0"
  }
}
